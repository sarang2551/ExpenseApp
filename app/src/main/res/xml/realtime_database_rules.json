{
  "rules": {
    "appData": {
      ".read": true,
      ".write": true,
      "transactions": {
        "$id": {
          ".validate": "newData.hasChildren(['amount','currency','type','categoryId','date'])"
        }
      }
    }
  }
}
